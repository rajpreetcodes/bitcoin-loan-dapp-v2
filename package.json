{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "bitcoin_loan_dapp",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "smoke": "start-server-and-test 'dfx start --clean --background' http://127.0.0.1:4943 'npm run dev & sleep 5 && npx cypress run --spec cypress/e2e/smoke.cy.js'"
  },
  "type": "module",
  "workspaces": [
    "src/bitcoin_loan_dapp_frontend"
  ],
  "devDependencies": {
    "cypress": "^14.5.1",
    "start-server-and-test": "^2.0.12"
  }
}
